import { MenuItem } from '~/src/utils/processMegaMenu';

interface NavbarOptionsItemsProps {
	'data-items'?: MenuItem[];
}

const NavbarOptionsItems: React.FC = props => {
	return <></>;
};

export default NavbarOptionsItems;
