import { styled } from '~/src/styles/stitches.config';

export const Container = styled('div', {
	display: 'flex',
	flexDirection: 'row',
	alignItems: 'center',
	maxWidth: '1280px',
	paddingX: 5,
});
