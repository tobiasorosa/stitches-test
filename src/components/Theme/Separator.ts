import { styled } from '~src/styles/stitches.config';

export const Separator = styled('hr', {
	marginTop: '2rem',
	opacity: 1,
	border: 0,
	borderColor: '#E5EFFF',
	borderStyle: 'solid',
	borderBottomWidth: '2px',
	width: '100%',
});
